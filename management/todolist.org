#+TITLE: lunixose website — project checklist
#+DATE: <2026-08-10 Mon>
#+TODO: TODO(t) | DONE(d!)

This file tracks the phased rebuild of the personal dashboard + blog.
Check off each item as it lands. Each phase ends with a commit.

* Architecture decisions (locked)
- Framework: Vite + React + TypeScript SPA + Node (h3) server on :3100
- Deploy: keep lunatix flake input + Caddy reverse_proxy (unchanged)
- Blog content: public notes (=#+public: t=) read live from /root/Notes
- Widgets: Forgejo + Syncthing live API proxies; Email = placeholder card
- Theme: Catppuccin Mocha, Obsidian-style shell, Tailwind v4 via =@catppuccin/tailwindcss=
- Dropped: auth, notes editor, graph/map right bar, all Vue code

* Phase 1 — Scaffold shell (DONE)
- [X] Remove Vue app (src/, server/, old configs, old package.json)
- [X] package.json with React 19 + Vite + Tailwind v4 + h3 + uniorg
- [X] vite.config.ts (+ /api dev proxy → :3100)
- [X] tsconfig split: tsconfig.app.json / tsconfig.server.json
- [X] Catppuccin Mocha global CSS (=src/assets/css/main.css=)
- [X] Layout: =AppShell= (collapsible sidebar) + =Pane=
- [X] UI: =WidgetCard=, hooks =useTheme= / =useWidgetStatus=
- [X] Views: =DashboardView=, =BlogView=, =ProjectsView= (placeholders)
- [X] Server: =h3= app, status API + SPA static fallback
- [X] typecheck + build pass locally, API verified with curl
- [ ] Commit: phase 1 scaffold

* Phase 2 — Management docs (IN PROGRESS)
- [X] =management/todolist.org= (this file)
- [X] =management/documentation.org= (learning curriculum)
- [ ] Commit: phase 2 docs

* Phase 3 — Server: notes metadata API + RSS
- [ ] =server/utils/notes.ts=: scan /root/Notes, parse Denote filenames + org front matter
- [ ] =#+public: t= gate; extract =#+title=, =#+filetags=, date, day
- [ ] =GET /api/notes= → public note list (sorted by date, paginated)
- [ ] =GET /api/notes/:id= → raw org content (id = filename slug)
- [ ] =GET /rss.xml= → RSS feed of public notes
- [ ] Update =BlogView= to render the list from the API
- [ ] typecheck + build, curl-verify endpoints
- [ ] Commit: phase 3

* Phase 4 — Blog list polish
- [ ] Tag filter (reuse =#+filetags= values)
- [ ] Pagination (notashelf-style "older/newer")
- [ ] Reading time + published date per list item
- [ ] Commit: phase 4

* Phase 5 — Post view (org → HTML)
- [ ] =PostView= fetches raw org, renders with uniorg (client-side)
- [ ] TOC from headings, in-page anchor nav
- [ ] Catppuccin typography for rendered org content
- [ ] Commit: phase 5

* Phase 6 — Dashboard widgets
- [ ] =server/utils/forgejo.ts= proxy → git.lunixose.duckdns.org/api/v1
- [ ] =server/utils/syncthing.ts= proxy → localhost:8384/rest (token via env)
- [ ] Wire =ForgejoWidget= / =SyncthingWidget=; keep =MailWidget= placeholder
- [ ] Commit: phase 6

* Phase 7 — Nix packaging + service
- [ ] Update =nix/package.nix= (new npmDepsHash, dist + dist-server)
- [ ] Update =website.nix= (drop ADMIN_PASSWORD + secrets; keep NOTES_DIR/ACLs)
- [ ] Local nix build check
- [ ] Commit: phase 7

* Phase 8 — Deploy
- [ ] Push website repo → bump flake input → =switch-oracle=
- [ ] Verify lunixose.duckdns.org: dashboard + blog + rss
- [ ] Commit any remaining changes

* Future (deferred)
- [ ] Wire Email widget to a real mail service (none exists yet)
- [ ] Projects page real content
- [ ] Theme toggle between mocha / latte (Catppuccin supports it)
